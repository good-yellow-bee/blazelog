run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  disable:
    - errcheck  # Disabled: codebase intentionally ignores many Write errors (common Go HTTP pattern)
  enable:
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - gofmt
    - goimports
    - misspell
    - unconvert
    - bodyclose
    - noctx
    - sqlclosecheck
    - gocritic
    - revive

linters-settings:
  gosec:
    severity: medium
    confidence: medium
    excludes:
      - G104 # Audit errors not checked (allow for deferred closes)
      - G106 # SSH InsecureIgnoreHostKey (used for optional host key validation)
      - G115 # Integer overflow conversion (safe for bounded values like buffer sizes)
      - G201 # SQL formatting (uses safe time functions, not user input)
      - G304 # File path provided as taint input (allow for config files)
      - G402 # TLS MinVersion (configurable, not a hardcoded vulnerability)
      - G404 # Weak random generator (acceptable for non-crypto uses like jitter)

  govet:
    enable-all: true
    disable:
      - shadow
      - fieldalignment

  revive:
    rules:
      - name: exported
        disabled: true
      - name: package-comments
        disabled: true

  gocritic:
    enabled-tags:
      - diagnostic
      - performance
    disabled-checks:
      - hugeParam
      - rangeValCopy
      - ifElseChain
      - appendCombine
      - assignOp
      - sprintfQuotedString

  misspell:
    locale: US

issues:
  exclude-rules:
    # Test files can have longer functions and more complexity
    - path: _test\.go
      linters:
        - gosec
        - gocritic
        - errcheck
        - unused

    # Generated proto files
    - path: \.pb\.go$
      linters:
        - all

    # Generated templ files
    - path: _templ\.go$
      linters:
        - all

  max-issues-per-linter: 50
  max-same-issues: 10
